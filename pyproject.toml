[project]
name = "gist-weather"
version = "0.1.0"
description = "A simple weather API built with FastAPI to comment on GitHub Gists."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.135.1",
    "pygithub>=2.8.1",
    "weather-sdk-challenge>=0.1.0",
]
